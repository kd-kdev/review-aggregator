# SteamReviews+

## A Steam review aggregator & sentiment analysis site!

Database is PostgreSQL, inserts pulled from SteamWebAPI using script `steam_ingest.py`
Backend made with Flask, pure JSON response API.
Frontend pending completion (temporarily uses HTMX + Jinja).
